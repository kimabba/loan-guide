{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "deploy:secrets": "wrangler secret put GEMINI_API_KEY && wrangler secret put FILE_SEARCH_STORE_NAME",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^0.11.0",
    "@supabase/supabase-js": "^2.42.0",
    "hono": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
